# Example secrets.yaml structure (UNENCRYPTED - for reference only)
#
# To create the real encrypted file:
# 1. First set up your age key (see README.md)
# 2. Update .sops.yaml with your public key
# 3. Run: nix-shell -p sops --run "sops secrets/secrets.yaml"
# 4. Paste content like below, save, and exit

ssh:
  id_rsa: |
    -----BEGIN OPENSSH PRIVATE KEY-----
    your-private-key-content-here
    -----END OPENSSH PRIVATE KEY-----
  id_rsa_pub: "ssh-rsa AAAA... comment"
